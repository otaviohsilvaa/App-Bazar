import { Text, View } from 'react-native';

export default function favoriteScreen() {
 return (
   <View className="items-center justify-center flex w-full h-full">
    <Text>Tela dos Favoritos</Text>
   </View>
  );
}